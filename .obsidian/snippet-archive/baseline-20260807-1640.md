# Snippet Baseline — 2026-08-07 16:40

## Active styling state

- `appearance.json` had **no** `enabledCssSnippets` and `.obsidian/snippets/` was empty.
- Theme: **Velocity 2.2.1** (light mode).
- `translucency: true`, `baseFontSize: 15`.
- Fonts: 苹方-简 / LXGW WenKai Mono / HarmonyOS Sans SC / FiraCode Nerd Font Mono.
- `defaultViewMode: preview`, `propertiesInDocument: hidden`, `readableLineLength: false`.

## Plan (克制知识工作台, light)

1. `wk-design-tokens.css` — design tokens (map theme vars, no hardcoded colors).
2. `wk-app-frame.css` — workspace tab headers + status bar.
3. `wk-note-surface.css` — note surface, typography, internal links, properties.
4. `wk-sidebar.css` — file explorer / side docks.

Verification: screenshot reading unavailable in this env — user visually verifies after Obsidian reload (Cmd+R / toggle snippets off-on).
